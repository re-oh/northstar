[package]
name = "northstar-editor-core"
description = "The editor View lifecycle contract: persistent state, activation, closing, serialization, workspace interaction. No UI library chosen yet — see docs/editor-views.md."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
thiserror = { workspace = true }
