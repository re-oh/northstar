[package]
name = "northstar-time"
description = "Northstar's simulation clock: the single authority for pause, time scale, and editor-preview time, layered over Bevy's own Time<Fixed>/Time<Virtual>."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
northstar-diagnostics = { workspace = true }
tracing = { workspace = true }
