[package]
name = "northstar"
description = "Top-level Northstar plugin: installs the foundational plugins (assets, time, diagnostics) and establishes ordered startup phases."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
bevy = { workspace = true, features = ["bevy_asset"] }
northstar-bevy = { workspace = true }
northstar-diagnostics = { workspace = true }
northstar-time = { workspace = true }
tracing = { workspace = true }
